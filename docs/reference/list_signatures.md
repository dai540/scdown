# List available signatures

List available signatures

## Usage

``` r
list_signatures(obj = NULL)
```

## Arguments

- obj:

  Optional `scdown_obj` with custom signatures.

## Value

A character vector.
