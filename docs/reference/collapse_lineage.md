# Collapse fine labels into major lineages

Collapse fine labels into major lineages

## Usage

``` r
collapse_lineage(obj, mapping = NULL)
```

## Arguments

- obj:

  A `scdown_obj`.

- mapping:

  Optional data frame with `pattern` and `lineage`.

## Value

The updated `scdown_obj`.
