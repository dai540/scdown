#' scdown: Simple Downstream Analysis After Cell Annotation
#'
#' `scdown` is a compact downstream toolkit for annotated single-cell RNA-seq
#' data. It standardizes a few common input types, keeps the internal
#' representation sparse-first, and separates lightweight exploration from
#' sample-aware testing: maps, markers, genes of interest, composition,
#' annotation checks, signatures, communication, and a compact report.
#'
#' @keywords internal
"_PACKAGE"
